//7. Write a program to enter an integer and print the sum of the digits in the integer
#include <stdio.h>

int main() {
    int num;
    scanf("%d",&num);
    int sum=0;
  while(num>0){
      sum=sum+(num%10);
      num=num/10;
}
printf("%d",sum);
    return 0;
}
