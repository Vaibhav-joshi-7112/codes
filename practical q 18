#include <stdio.h>

int main() {
   char st[1000]; 
   gets(st);
   char ch;
  scanf("%c",&ch);
   int count=0;int i=0;
   while(st[i]!='\0'){
       if(ch==st[i]){
           count++;
           
       }
       i++;
   }
printf("occurence of the character is : %d",count);
    return 0;
}
